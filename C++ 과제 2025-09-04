→ C++ for문
#include <iostream>

int main()
{
    for (int i = 0; i < 10; ++i) /* for 반복문
    {
        std::cout << "Hello World" << std::endl;
    }

    return 0;
}

------------------------------------------------------------------------------

----------→ 오름차순, 내림차순 정렬

#include <stdio.h>                   // 표준 입출력 헤더 파일 포함
#define SIZE 3                       // 사람이 3명임을 상수로 정의

typedef struct {                     // 체중정보를 저장할 구조체 정의
    char name[10];                   // 이름을 저장할 문자 배열
    double w;                        // 몸무게를 저장할 실수형 변수
} WEIGHT;

void swap(WEIGHT *, WEIGHT *);       // 두 사람의 정보를 바꾸는 함수 선언

int main(void) {                     
    WEIGHT man[SIZE] = {             // 3명의 이름과 몸무게 초기화
        {"한개발", 57.5},
        {"엄청군", 125.6},
        {"갈비양", 35.7} 
    };
    int i, j;                        // 반복문에서 사용할 변수 선언

    // 몸무게 오름차순으로 정렬(Bubble sort의 일부)
    for(i = 0; i < 2; i++) {         
        for(j = i + 1; j < 3; j++) {
            if(man[i].w > man[j].w) { // 앞사람이 더 무거우면
                swap(&man[i], &man[j]); // 두 사람 순서 바꿈
            }
        }
    }

    printf(" 이름 \t체중\n");         // 표 머리글 출력
    for(i = 0; i < 3; i++) {         // 정렬된 결과 출력
        printf(" %s %5.1f\n", man[i].name, man[i].w); // 이름과 무게를 출력
    }
    return 0;                        // 프로그램 정상 종료
}

void swap(WEIGHT *mani, WEIGHT *manj) // 두 구조체 변수의 값을 교환하는 함수
{
    WEIGHT temp;                      // 임시로 값을 보관할 구조체 변수 선언
    temp = *mani;                     // mani의 값 temp에 저장
    *mani = *manj;                    // manj의 값을 mani에 복사
    *manj = temp;                     // temp의 값을 manj에 저장
}

----------→ 비교 연산자를 바꿔줌

if(man[i].w < man[j].w){
    swap(&man[i], &man[j]);
}
if(man[i].w > man[j].w){
    swap(&man[i], &man[j]);
}
기존: if(man[i].w < man[j].w) → 내림차순
변경: if(man[i].w > man[j].w) → 오름차순
→ i번째 요소가 j번째 요소보다 더 크면 둘의 순서를 바꿔서, 배열 앞쪽에 항상 작은 값이 오도록 정렬됩니다.



----------→ 소괄호, 중괄호, 대괄호

1. 소괄호 ( )
용도
함수 호출/선언에서 매개변수를 감싸는 데 사용
연산 우선순위를 지정할 때 사용
조건문, 반복문에서 조건식이나 제어식 감쌈
예시
함수 호출: printf("Hello");
함수 선언: int add(int a, int b)
조건문: if (x > 10)
연산 우선: result = (a + b) * c

2. 중괄호 { }
용도
코드 블록, 영역을 정의
함수, 조건문, 반복문 등에서 실행될 명령문 집합을 감쌈
여러 문장을 하나의 단위로 묶어줌
예시
C
if (a > b) {
    printf("a가 큽니다.");
}
for (int i = 0; i < 3; i++) {
    printf("%d\n", i);
}

3. 대괄호 [ ]
용도
배열 선언/접근할 때 인덱스를 감쌈
일부 언어에서는 리스트, 인덱스, 속성 접근 등에도 사용
예시
C
int arr[10]; // 크기가 10인 배열 선언
arr[0] = 5;  // 첫 번째 요소에 값 할당

요약
( ) 소괄호: 함수, 조건, 연산 우선 순위 등 값을 그룹으로 묶음
{ } 중괄호: 코드 영역을 정의하는 블록(집합)
[ ] 대괄호: 배열, 리스트, 인덱스 등에 접근하는 용도

----------→ 대략적인 예약어 사용 순위

C++ 예약어 사용 순위 (대략적/구현에 따라 다름, C 예약어 포함)
int : 변수, 함수 반환형
return : 함수 반환
if : 조건문
for : 반복문
while : 반복문
void : 함수 반환형
else : 조건문
class : 클래스 선언
public/private/protected : 접근 지정자
char/string : 문자, 문자열 선언
namespace : 네임스페이스 선언
switch/case/break : 다중 분기/탈출
double/float: 실수형 선언
struct : 구조체 선언
virtual : 가상 함수 선언
template : 제네릭 함수/클래스 선언
const : 상수 선언
new/delete : 동적 메모리 관리
try/catch/throw : 예외처리
sizeof : 자료형 크기 판단

----------→ 생성형 AI의 최대 활용 방법

나는 오늘 C++을 처음 배웠어. 근데 9월 한달동안 간단한 게임을 C++로 만들면서 C++을 이해하고 싶어. 
무슨 게임을 만들면 좋을지 추천해주고, 9월 동안의 일정을 상세하게 작성해줘.

정말 멋진 목표예요!
C++을 배우면서 간단한 게임을 만들어보는 것은 실력 향상에 큰 도움이 됩니다.
아래에 초보자에게 적합한 게임 추천과 9월 한 달간의 C++ 공부 및 게임 개발 일정을 상세하게 제안해드릴게요.

= 추천 제안과 일정 제시
